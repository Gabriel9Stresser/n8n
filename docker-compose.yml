services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - GENERIC_TIMEZONE=America/Sao_Paulo
      - TZ=America/Sao_Paulo
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
      # Necessário para API – Fila de Aprovação e API – Etapas do Maria:
      - N8N_BASE_URL=http://localhost:5678
      # Obtenha em: n8n → Settings → API → Create API Key
      - N8N_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJmNDNiNWYyMC04MzRkLTQzOTMtOTE4NS1hNzlhZDdiZjZhNTQiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwianRpIjoiM2ZlM2IyODEtMTQxMC00ZWY0LWFkZjgtMDc2NGYxZjhkZWEyIiwiaWF0IjoxNzcwMzc3MTczLCJleHAiOjE3NzI5Mzg4MDB9.UMW1iYQLYn1thwhQDIaFu-fqfObidvinhBnIvENUlvA
    volumes:
      - n8n_data:/home/node/.n8n
      - ./workflows:/workflows:ro

  frontend:
    build:
      context: ./frontend-react
      dockerfile: Dockerfile
      args:
        VITE_N8N_BASE: ""
    depends_on:
      - n8n
    container_name: n8n-survey
    restart: unless-stopped
    ports:
      - "3000:80"

volumes:
  n8n_data:
